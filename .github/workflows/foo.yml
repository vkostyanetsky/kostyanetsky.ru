name: Build & Deploy

on:
  workflow_dispatch:

jobs:
  
  build:
    
    runs-on: ubuntu-latest

    steps:      
      - uses: actions/checkout@v2

      - name: Tell something
        run: |
          TELEGRAM_MESSAGE="111"
          curl 'https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage?text={{ $TELEGRAM_MESSAGE }}&chat_id=${{ secrets.TELEGRAM_CHAT_ID }}'

      - name: Tell something
        run: |
